---
const { platform, username } = Astro.props;
---

<a class="social-media" href={`https://www.${platform}.com/${username}`}>{platform}</a>

<style>
    .social-media {
        padding: 0.5rem 1rem;
        color: white;
        background-color: #4c1d95;
    }
</style>